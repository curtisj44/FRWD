(function (frwd, $) {
	'use strict';

	/* TODO - CJ - clean up / delete? */

	/*
	frwd.fixIE7Grid = function () {
		var $html = $('html');

		if ($html.hasClass('ie7')) {
			$html.find('.region:last-child').not('.region-centered').addClass('region-last');
		}
	};
	*/

	//$(frwd.fixIE7Grid);
}(window.frwd = window.frwd || {}, jQuery));